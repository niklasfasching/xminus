name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GOHEADLESS_EXECUTABLE: google-chrome
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v2
    - run: make install test
